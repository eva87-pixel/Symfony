<form method="post" action="{{ path('app_distributeur_delete', {'id': distributeur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ distributeur.id) }}">
    <button class="btn">Delete</button>
</form>
