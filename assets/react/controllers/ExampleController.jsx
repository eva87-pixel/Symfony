import React from 'react';

export default function ExampleController() {
    return (
        <div>
            <h2>Hello from React!</h2>
            <p>This is a simple React controller rendered in Symfony.</p>
        </div>
    );
}